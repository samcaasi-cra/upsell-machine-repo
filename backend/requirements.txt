# These versions are the set actually verified working together. The MCP SDK drags
# starlette and uvicorn forward, and fastapi has to move with them: fastapi 0.115
# rejects starlette 1.6 outright ("Router.__init__() got an unexpected keyword
# argument 'on_startup'"). Pinning any one of the four older breaks installation.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.1
pydantic==2.13.4

requests==2.32.3
python-dotenv==1.0.1
beautifulsoup4==4.12.3
lxml==5.3.0

# Powers Today, Ask, and the automated research extraction.
openai>=2.0.0

# Exposes the agent tools over the Model Context Protocol (see MCP.md).
mcp>=2.0.0
