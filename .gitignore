.env
.env.local
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
build/
.DS_Store
Thumbs.db
.vscode/
.idea/

# Runtime-generated cache from the decision-maker research import flow
backend/data/decision_makers/*.json

# Runtime-generated cache tracking which individuals have already been seen per
# customer, used to detect first-time logins
backend/data/usage_individuals/*.json

# Runtime-generated cache from the news research import flow (acquisitions,
# new offices, product launches)
backend/data/news_events/*.json

# Runtime state for the daily research scheduler (last-run tracking)
backend/data/research_schedule.json

# Contains hardcoded live-looking API keys (SSC token + OpenAI key) -- never commit.
# Left in place at the user's request rather than stripped/edited.
Cyber_Rescue_Supply_Chain_Crisis_Response_Plan.ipynb
